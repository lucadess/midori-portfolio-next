export default function HomeWorkCategory({children}){
    return(
        <div className="home-work-category flex flex-col items-center justify-center" role="button">
            {children}
        </div>
    )
}